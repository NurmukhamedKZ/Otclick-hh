# Loaded ONLY by vitest (Vite mode=test) — dummy values so the env-dependent
# api/session-sync tests run deterministically without a developer .env.
# wxt dev/build use .env / .env.production and never read this file.
VITE_SUPABASE_URL=https://test-project.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
VITE_API_BASE=http://localhost:8000
VITE_APP_BASE=http://localhost:3000
