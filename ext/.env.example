# Local Otclick-HH stack. Copy to ext/.env and fill the anon key with
# SUPABASE_ANON_KEY from the repo-root .env (both are signed by one JWT_SECRET).
VITE_API_BASE=http://localhost:8000
VITE_APP_BASE=http://localhost:3000
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=
